#!/usr/bin/env bash
set -euo pipefail

APP_DIR=${1:-}

# Verifica se há variavel BUILDPACK_SSH_KEY
if [ -z "${BUILDPACK_SSH_KEY:-}" ]; then
  echo "SSH Key Buildpack: BUILDPACK_SSH_KEY não configurada" >&2
  exit 100
fi

echo "SSH Key Buildpack"
exit 0
